[project]
name = "arch-config"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Fernando Borquez", email = "fernandoborquez215@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
arch-config = "arch_config:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
